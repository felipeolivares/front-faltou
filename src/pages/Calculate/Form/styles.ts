import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  paperCalc: {
    margin: "6px",
    padding: "16px",
    height: "auto",
  },
}));

export default useStyles;
